query SearchContentData($search: String, $limit: Int, $skip: Int) {
  ContentData(
    where: { SearchTitle: { contains: $search } }
    limit: $limit
    skip: $skip
  ) {
    total
    items {
      SearchTitle
      SearchDescription
      RelativePath
      Url
    }
  }
}